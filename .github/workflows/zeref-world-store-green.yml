name: ZEREF WORLD STORE GREEN

on:
  push:
    branches: [zeref-world-knowledge-r12-train-001]
    paths:
      - 'beastbox/world_knowledge.py'
      - 'tests/test_world_knowledge_store.py'
      - '.github/workflows/zeref-world-store-green.yml'

permissions:
  contents: read

jobs:
  green:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install test runtime
        run: |
          python -m pip install -e '.[dev]'
          python -m pip install torch --index-url https://download.pytorch.org/whl/cpu
      - name: Focused world store
        run: python -m pytest -q tests/test_world_knowledge_store.py
      - name: Complete regression suite
        run: python -m pytest -q
