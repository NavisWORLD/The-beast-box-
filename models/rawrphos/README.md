# RAWRPHØS

Pronounced RAWR-fohs. Canonical model ID: `rawrphos-native`. Family: `RAWRPHOS`.
Cory Davis / COSMOS / Davis Cosmic Synapse Theory.

Native PHOS-derived neural research, independently packaged as `rawrphos`.
Model weights, persistent COSMOS memory and host authority remain separate.
Historical PHOS, SAMGO and Zeref artifacts retain their original identities.
See `PROGRESS.md` for verified state and next execution action.

The package consolidates closely related code instead of creating empty files
for every proposed filename. All model code remains here; existing Beast Box
owns runtime memory, R12 routing, CNS and policy.
