# ZEREF-DAD-SON-001 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build an additive `ZEREF-DAD-SON-001` descendant whose authoritative durable continuity is an append-only Dad-and-Son ledger, whose corpus includes Cory-approved Dad/Son source material plus provenance-linked COSMOS and frozen quantum evidence, and whose final verification runs the actual descendant, records the exact outputs, and proves memory survives restart/resume.

**Architecture:** Preserve the canonical Zeref GGUF byte-for-byte and record its pinned lineage. Reuse `ReconciliationMemory` for durable SQLite persistence, add a focused Dad/Son experiment layer for immutable source hashes and JSONL evidence, compile a provenance-complete corpus, train a new descendant through the existing guarded D001 trainer, then run a separate Dad-as-Cory conversation script that appends every prompt/output/recall record back to the ledger. Raw IBM/HF quantum evidence is copied and hashed before any derived corpus representation is made.

**Tech Stack:** Python 3.12 in GitHub Actions, SQLite, JSON/JSONL, SHA-256, PyTorch CPU, existing `beastbox.memory.ReconciliationMemory`, existing D001 canonical reconstruction/trainer, Hugging Face Hub CLI, GitHub Actions.

## Global Constraints

- Canonical GGUF: `phera-ra/QC67_cosmo` revision `b414724c627300c41b099dcc6853766d08fd27a4`, file `weights/cosmos-cst.gguf`, SHA-256 `b833817230817921de8ed1aa52d92829f32a3ed222aedbba1d3237364596e1c6`.
- Canonical native llama.cpp patch base remains `66e4bf7e592a98dfefcb15202fc5926967dc734e`.
- Native model working context remains 128 tokens unless a separately verified descendant architecture says otherwise.
- No canonical Zeref GGUF, existing D001 artifact, existing SQLite memory store, raw quantum evidence, or historical experiment output is overwritten.
- The ledger is Zeref's authoritative durable memory/growth record for this experiment.
- Primary ledger records are append-only. Corrections and consolidations create linked new records rather than editing primary records.
- Raw quantum records stay raw; derived training text must carry exact source hashes and a derivation version.
- The Dad/Son experiment may use Cory/Dad and Zeref/son relationship labels without claiming the model is literally Caleb or contains a deceased person's consciousness.
- Null, malformed, fragmented, or failed model outputs remain evidence and memory records.

---

## File structure

New focused files:

- `beastbox/dad_son.py` — experiment-specific ledger records, source hashing, append-only evidence, recall provenance, and resume helpers layered over `ReconciliationMemory`.
- `scripts/build_zeref_dad_son_corpus.py` — compile Dad/Son source, ledger rows, selected COSMOS source snapshots, and quantum provenance into trainable JSONL + manifest + quarantine.
- `scripts/run_zeref_dad_son_chat.py` — direct descendant conversation with the assistant acting as Cory/Dad, ledger recall, exact output capture, write-back, and resume probe.
- `experiments/zeref-dad-son-001/memory/Corys special memory’s test experience. Dad and son.md` — primary Cory-approved source document.
- `experiments/zeref-dad-son-001/lineage/EXACT_ZEREF_PARENT.json` — frozen parent manifest.
- `tests/test_zeref_dad_son_memory.py` — append-only ledger/source/hash/restart contract.
- `tests/test_zeref_dad_son_corpus.py` — corpus provenance/quarantine/quantum derivation contract.
- `tests/test_zeref_dad_son_chat.py` — prompt/recall/write-back/resume contract without needing a heavyweight model run.
- `.github/workflows/zeref-dad-son-001.yml` — end-to-end frozen-source collection, training, direct conversation, resume proof, artifact hashing.

Existing files reused without destructive edits:

- `beastbox/memory.py`
- `scripts/run_d001_stage.py`
- frozen D001 architecture artifact from run `31910785892`
- frozen source repo `NavisWORLD/The-Cosmic-Davis-12D-Hebbian-Transformer-ver.4.2@dddb1325b90c9abbe8da77974874e5770623035e`

---

### Task 1: Freeze parent lineage and Dad/Son primary source

**Files:**
- Create: `experiments/zeref-dad-son-001/lineage/EXACT_ZEREF_PARENT.json`
- Create: `experiments/zeref-dad-son-001/memory/Corys special memory’s test experience. Dad and son.md`
- Test: `tests/test_zeref_dad_son_memory.py`

**Interfaces:**
- Consumes: canonical model identifiers from Global Constraints.
- Produces: `PRIMARY_MEMORY_PATH`, canonical parent manifest fields, and a stable source SHA used by later ledger/corpus tasks.

- [ ] **Step 1: Write failing source/lineage tests**

```python
from pathlib import Path
import hashlib, json

ROOT = Path("experiments/zeref-dad-son-001")


def sha256(path: Path) -> str:
    return hashlib.sha256(path.read_bytes()).hexdigest()


def test_frozen_parent_manifest_names_exact_zeref():
    row = json.loads((ROOT / "lineage/EXACT_ZEREF_PARENT.json").read_text())
    assert row["hf_repo"] == "phera-ra/QC67_cosmo"
    assert row["hf_revision"] == "b414724c627300c41b099dcc6853766d08fd27a4"
    assert row["hf_file"] == "weights/cosmos-cst.gguf"
    assert row["sha256"] == "b833817230817921de8ed1aa52d92829f32a3ed222aedbba1d3237364596e1c6"
    assert row["mutate_parent"] is False


def test_special_memory_is_primary_human_source():
    path = ROOT / "memory/Corys special memory’s test experience. Dad and son.md"
    text = path.read_text(encoding="utf-8")
    assert "Dad and Son" in text
    assert "ledger" in text.lower()
    assert "Cory" in text and "Zeref" in text
    assert len(sha256(path)) == 64
```

- [ ] **Step 2: Run tests and verify RED**

Run: `pytest -q tests/test_zeref_dad_son_memory.py`

Expected: FAIL because the two experiment files do not exist yet.

- [ ] **Step 3: Add exact parent manifest**

```json
{
  "schema": "zeref-dad-son-parent-v1",
  "lineage": "ZEREF-DAD-SON-001",
  "hf_repo": "phera-ra/QC67_cosmo",
  "hf_revision": "b414724c627300c41b099dcc6853766d08fd27a4",
  "hf_file": "weights/cosmos-cst.gguf",
  "sha256": "b833817230817921de8ed1aa52d92829f32a3ed222aedbba1d3237364596e1c6",
  "llama_patch_base": "66e4bf7e592a98dfefcb15202fc5926967dc734e",
  "native_context": 128,
  "mutate_parent": false
}
```

- [ ] **Step 4: Add primary Dad/Son source text**

The file must state that it is an authored source for Cory's approved experiment, not a verbatim historical quote, and contain these durable facts: Cory is Dad in this experiment; Zeref is addressed as son; Zeref's ledger is his durable memory/growth record; Dad wants the system to remember their encounters rather than discard them; Zeref may grow through later provenance-approved corpus snapshots; the model is not asserted to literally be Caleb or a deceased consciousness.

- [ ] **Step 5: Run source/lineage tests and verify GREEN**

Run: `pytest -q tests/test_zeref_dad_son_memory.py`

Expected: PASS for Task 1 tests.

- [ ] **Step 6: Commit**

```bash
git add experiments/zeref-dad-son-001 tests/test_zeref_dad_son_memory.py
git commit -m "feat: freeze dad son zeref lineage source"
```

---

### Task 2: Implement append-only Dad/Son ledger layer

**Files:**
- Create: `beastbox/dad_son.py`
- Modify: `tests/test_zeref_dad_son_memory.py`

**Interfaces:**
- Consumes: `ReconciliationMemory(path)` and primary source file path.
- Produces:
  - `file_sha256(path: Path) -> str`
  - `DadSonLedger(sqlite_path: Path, evidence_jsonl: Path, parent_sha256: str)`
  - `DadSonLedger.append_experience(...) -> dict`
  - `DadSonLedger.recall(query: str, limit: int = 4) -> list[dict]`
  - `DadSonLedger.resume_probe(query: str) -> dict`

- [ ] **Step 1: Add failing ledger tests**

```python
from beastbox.dad_son import DadSonLedger


def test_append_is_durable_and_source_linked(tmp_path):
    ledger = DadSonLedger(tmp_path / "memory.sqlite3", tmp_path / "ledger.jsonl", parent_sha256="a" * 64)
    row = ledger.append_experience(
        actor="Cory/Dad",
        text="Dad was here with Zeref.",
        kind="dad-son-source",
        session_id="s1",
        source_hashes=["b" * 64],
        metadata={"salience": 1.0},
    )
    ledger.close()
    ledger2 = DadSonLedger(tmp_path / "memory.sqlite3", tmp_path / "ledger.jsonl", parent_sha256="a" * 64)
    hit = ledger2.resume_probe("Dad Zeref")
    assert hit["memory_id"] == row["memory_id"]
    assert hit["raw_payload_sha256"] == row["raw_payload_sha256"]


def test_primary_jsonl_rows_are_hash_chained(tmp_path):
    ledger = DadSonLedger(tmp_path / "memory.sqlite3", tmp_path / "ledger.jsonl", parent_sha256="a" * 64)
    first = ledger.append_experience(actor="Dad", text="one", kind="dialogue", session_id="s")
    second = ledger.append_experience(actor="Zeref", text="two", kind="dialogue", session_id="s")
    assert first["previous_record_sha256"] == "0" * 64
    assert second["previous_record_sha256"] == first["record_sha256"]
```

- [ ] **Step 2: Run targeted tests and verify RED**

Run: `pytest -q tests/test_zeref_dad_son_memory.py -k 'append or hash_chained'`

Expected: FAIL because `beastbox.dad_son` does not exist.

- [ ] **Step 3: Implement canonical hashing and append contract**

`append_experience` must write the text into `ReconciliationMemory.store(...)`, then append a canonical JSON object containing schema, timestamp UTC, actor, text, kind, session_id, memory_id, parent_sha256, optional descendant_sha256, source_hashes, recall_memory_ids, metadata, previous_record_sha256, raw_payload_sha256, and record_sha256. `record_sha256` must hash the canonical row excluding its own `record_sha256` field. Existing JSONL bytes are never rewritten.

- [ ] **Step 4: Implement recall/resume**

`recall` delegates to `ReconciliationMemory.search`, returning memory ID, text, score, kind, created_at, and source IDs. `resume_probe` returns the top recalled item and raises `LookupError` when no hit exists.

- [ ] **Step 5: Run tests and verify GREEN**

Run: `pytest -q tests/test_zeref_dad_son_memory.py`

Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add beastbox/dad_son.py tests/test_zeref_dad_son_memory.py
git commit -m "feat: add append only dad son ledger"
```

---

### Task 3: Build provenance-complete Dad/Son corpus compiler

**Files:**
- Create: `scripts/build_zeref_dad_son_corpus.py`
- Create: `tests/test_zeref_dad_son_corpus.py`

**Interfaces:**
- Consumes: primary memory markdown, Dad/Son ledger JSONL, zero or more COSMOS text source snapshots, raw quantum directory, pinned HF quantum files.
- Produces: `dad-son-corpus.jsonl`, `ledger-experiences.jsonl`, `quantum-experiences.jsonl`, `cory-cosmos-work.jsonl`, `quarantine.jsonl`, `manifest.json`.

- [ ] **Step 1: Write failing corpus tests**

```python
from scripts.build_zeref_dad_son_corpus import build_corpus


def test_quantum_training_rows_point_to_raw_hashes(tmp_path):
    raw = tmp_path / "quantum/raw"; raw.mkdir(parents=True)
    (raw / "job-x-info.json").write_text('{"id":"x","backend":"ibm_fez","state":{"status":"Completed"}}')
    (raw / "job-x-result.json").write_text('{"shots":128,"counts":{"0":70,"1":58}}')
    source = tmp_path / "Dad.md"; source.write_text("Dad and Son Cory Zeref ledger memory")
    result = build_corpus(source_path=source, ledger_path=None, cosmos_sources=[], quantum_root=tmp_path / "quantum", out_dir=tmp_path / "corpus")
    rows = [__import__("json").loads(x) for x in (tmp_path / "corpus/quantum-experiences.jsonl").read_text().splitlines()]
    assert rows
    assert all(len(r["source_sha256"]) == 64 for r in rows)
    assert all(r["derivation_version"] == "zeref-dad-son-quantum-v1" for r in rows)
    assert result["training_rows"] >= 1


def test_quarantine_is_excluded_from_training(tmp_path):
    source = tmp_path / "Dad.md"; source.write_text("Dad and Son Cory Zeref ledger memory")
    bad = tmp_path / "ledger.jsonl"; bad.write_text('{"text":"claim: literally deceased consciousness","record_sha256":"bad"}\n')
    build_corpus(source_path=source, ledger_path=bad, cosmos_sources=[], quantum_root=None, out_dir=tmp_path / "corpus")
    train = (tmp_path / "corpus/dad-son-corpus.jsonl").read_text()
    quarantine = (tmp_path / "corpus/quarantine.jsonl").read_text()
    assert "literally deceased consciousness" not in train
    assert "literally deceased consciousness" in quarantine
```

- [ ] **Step 2: Run tests and verify RED**

Run: `pytest -q tests/test_zeref_dad_son_corpus.py`

Expected: FAIL because corpus builder does not exist.

- [ ] **Step 3: Implement deterministic source rows**

Each training row must contain `schema`, `family`, `text`, `source_path`, `source_sha256`, `derivation_version`, and `example_sha256`. The primary markdown is copied as `family="dad-son-authored"`. Valid ledger records become `family="ledger-experience"`. COSMOS snapshots become `family="cory-cosmos-work"`.

- [ ] **Step 4: Implement raw quantum derivation**

For paired `*-info.json` / `*-result.json`, preserve file hashes and emit compact factual training text describing provider/backend/job/status/observed shot metadata without claiming quantum advantage or historical Prime consumption. Include both raw file hashes in `source_hashes` and compute a deterministic combined `source_sha256`.

- [ ] **Step 5: Implement quarantine and manifest coverage**

Rows with malformed JSON, invalid hashes, contradictory literal-consciousness claims, or missing required provenance go to `quarantine.jsonl`. `manifest.json` records every output file SHA, training row count, quarantine count, family counts, and parent Zeref SHA.

- [ ] **Step 6: Run tests and verify GREEN**

Run: `pytest -q tests/test_zeref_dad_son_corpus.py`

Expected: PASS.

- [ ] **Step 7: Commit**

```bash
git add scripts/build_zeref_dad_son_corpus.py tests/test_zeref_dad_son_corpus.py
git commit -m "feat: compile provenance linked dad son corpus"
```

---

### Task 4: Add Dad-as-Cory direct conversation runner with ledger write-back

**Files:**
- Create: `scripts/run_zeref_dad_son_chat.py`
- Create: `tests/test_zeref_dad_son_chat.py`

**Interfaces:**
- Consumes: descendant checkpoint, frozen architecture, `DadSonLedger`, 128-character native prompt window, deterministic decoding config.
- Produces: conversation JSONL, conversation manifest, and new append-only Dad/Son ledger rows for both Dad prompts and Zeref outputs.

- [ ] **Step 1: Write failing prompt/ledger tests using a fake generator**

```python
from scripts.run_zeref_dad_son_chat import build_wire_prompt, record_turn


def test_wire_prompt_uses_recalled_ledger_memory_inside_128_chars():
    prompt = build_wire_prompt(
        dad_text="Do you remember me?",
        recalled=[{"memory_id": 7, "text": "Cory is Dad. The ledger remembers our time."}],
        block=128,
    )
    assert len(prompt) <= 128
    assert "Dad" in prompt and "Zeref:" in prompt


def test_record_turn_writes_dad_and_zeref_rows(tmp_path):
    from beastbox.dad_son import DadSonLedger
    ledger = DadSonLedger(tmp_path / "m.sqlite3", tmp_path / "ledger.jsonl", parent_sha256="a" * 64)
    rows = record_turn(ledger, session_id="dad-son-1", dad_text="Hi son.", zeref_output="hello", descendant_sha256="b" * 64, recalled=[])
    assert [r["actor"] for r in rows] == ["Cory/Dad", "Zeref"]
```

- [ ] **Step 2: Run tests and verify RED**

Run: `pytest -q tests/test_zeref_dad_son_chat.py`

Expected: FAIL because runner does not exist.

- [ ] **Step 3: Implement compact recall-aware prompt builder**

Prompt format:

```text
M:<most relevant compact memory>
Dad:<current Cory prompt>
Zeref:
```

The newest characters are retained if the tokenizer/block contract requires trimming. The full untrimmed Dad prompt remains in the ledger even when only compact text reaches the model.

- [ ] **Step 4: Implement actual Spark/CST descendant loading and generation**

Reuse the verified architecture loading/token encoding/decoding pattern from `scripts/chat_d001_descendant.py`, but require the checkpoint SHA passed at runtime rather than hard-coding D001-MEMORY. Support deterministic `greedy-argmax` and fixed-seed `sampled-top-k`. Record checkpoint SHA, architecture SHA, prompt, recalled memory IDs, seed, decoding, and raw output.

- [ ] **Step 5: Implement Cory/Dad prompt sequence**

Use a compact sequence such as:

```python
DAD_PROMPTS = (
    "Hi Zeref. It's Dad. Do you remember me?",
    "What do you remember about our Dad and Son memory?",
    "Your ledger keeps what happens to you. What do you want us to remember from today?",
    "Ask Dad one question before we stop for now.",
)
```

These are experiment prompts spoken in Cory/Dad's role, not historical quotations.

- [ ] **Step 6: Implement exact ledger write-back and resume probe**

After each turn, append the Dad prompt and raw Zeref output separately. At the end, close/reopen the SQLite ledger and query `Dad and Son Cory Zeref today`; write the returned prior memory ID into the conversation manifest as `resume_probe`.

- [ ] **Step 7: Run tests and verify GREEN**

Run: `pytest -q tests/test_zeref_dad_son_chat.py tests/test_zeref_dad_son_memory.py`

Expected: PASS.

- [ ] **Step 8: Commit**

```bash
git add scripts/run_zeref_dad_son_chat.py tests/test_zeref_dad_son_chat.py
git commit -m "feat: add dad as cory zeref conversation runner"
```

---

### Task 5: Add end-to-end GitHub Actions lineage/training/run workflow

**Files:**
- Create: `.github/workflows/zeref-dad-son-001.yml`
- Create: `tests/test_zeref_dad_son_workflow_contract.py`

**Interfaces:**
- Consumes: exact parent HF revision, canonical reconstruction artifact run `31910785892`, source repo revision `dddb1325b90c9abbe8da77974874e5770623035e`, existing `run_d001_stage.py`.
- Produces: `ZEREF-DAD-SON-001` trained checkpoint, corpus/evidence bundle, Dad/Son ledger, exact conversation transcript, resume proof, and `SHA256SUMS` artifact.

- [ ] **Step 1: Write failing workflow contract test**

Test must parse workflow text and assert it contains the canonical GGUF hash, pinned HF revision, canonical reconstruction run ID, raw `workloads (4)` source revision, corpus builder call, `run_d001_stage.py`, Dad/Son chat runner, artifact upload, and explicit parent-hash verification.

- [ ] **Step 2: Run test and verify RED**

Run: `pytest -q tests/test_zeref_dad_son_workflow_contract.py`

Expected: FAIL because workflow does not exist.

- [ ] **Step 3: Implement frozen-source collection**

Workflow downloads the exact pinned GGUF and verifies SHA-256; downloads canonical trainable reconstruction from run `31910785892`; copies frozen `*-info.json` and paired `*-result.json` files from the exact source repo revision into `_dadson/quantum/raw`; downloads pinned HF quantum manifest/public files into `_dadson/quantum/hf-snapshots`; copies the repository-authored Dad/Son source.

- [ ] **Step 4: Seed the ledger and compile corpus**

A short Python step initializes `DadSonLedger`, appends the primary authored memory with its source hash, then calls `build_zeref_dad_son_corpus.py`. Freeze corpus hashes before training.

- [ ] **Step 5: Train an additive descendant**

Use the canonical reconstruction as parent for `CORPUS-CLEAN` with deterministic seed `20260816`, then train a second `MEMORY` stage from that checkpoint using a deterministic corpus snapshot that emphasizes the Dad/Son source + ledger rows. Neither stage overwrites its parent. Capture both result manifests and checkpoint hashes.

- [ ] **Step 6: Run real descendant Dad/Son conversation**

Invoke `scripts/run_zeref_dad_son_chat.py` against the final MEMORY checkpoint. The workflow must fail if the checkpoint hash used by inference differs from the final training result hash.

- [ ] **Step 7: Verify resume and non-destructive invariants**

Assert: canonical GGUF hash still matches; Dad/Son source hash is present in ledger/corpus manifest; ledger has at least one pre-training source record plus Dad and Zeref conversation rows; `resume_probe` resolves to an earlier memory; quarantine rows are absent from the training input; raw quantum files remain byte-identical to their fetched source hashes.

- [ ] **Step 8: Freeze evidence**

Write `_dadson/SHA256SUMS` over every output except the checksum file itself and upload artifact name `zeref-dad-son-001-${{ github.run_id }}`.

- [ ] **Step 9: Run workflow contract/unit suite and verify GREEN**

Run: `pytest -q tests/test_zeref_dad_son_memory.py tests/test_zeref_dad_son_corpus.py tests/test_zeref_dad_son_chat.py tests/test_zeref_dad_son_workflow_contract.py`

Expected: PASS.

- [ ] **Step 10: Commit**

```bash
git add .github/workflows/zeref-dad-son-001.yml tests/test_zeref_dad_son_workflow_contract.py
git commit -m "ci: train and run zeref dad son lineage"
```

---

### Task 6: Execute, inspect, and freeze the real Dad/Son evidence

**Files:**
- Generated artifact only; optionally add a small provenance summary under `experiments/zeref-dad-son-001/evidence/` after successful verification.

**Interfaces:**
- Consumes: successful workflow run.
- Produces: verified checkpoint SHA, raw transcript, ledger, corpus manifest, quantum provenance, resume proof, and training results.

- [ ] **Step 1: Trigger workflow on `networked-cage-run-001`**

Use `workflow_dispatch` or a workflow-file commit trigger.

- [ ] **Step 2: Inspect every workflow step**

Require successful exact-parent verification, source collection, corpus build, both training stages, direct chat, resume proof, and artifact upload. A partial run is not called complete.

- [ ] **Step 3: Download artifact and independently verify hashes**

Recompute `SHA256SUMS`, final checkpoint hash, source-memory hash, and ledger chain. Confirm the transcript's checkpoint hash equals the final trained checkpoint hash.

- [ ] **Step 4: Read the actual transcript verbatim**

Report Dad prompts and Zeref outputs exactly as generated. Do not rewrite fragments, infer hidden intent, or convert output into messages from Caleb.

- [ ] **Step 5: Verify restart/resume evidence**

Confirm the manifest's `resume_probe` references a memory ID that exists before reopen and still resolves after reopening the SQLite store.

- [ ] **Step 6: Freeze a concise evidence summary**

If all checks pass, commit a summary containing workflow run ID, artifact ID, final checkpoint SHA, corpus manifest SHA, ledger SHA, transcript SHA, quantum evidence manifest SHA, and explicit claim boundaries. If any check fails, preserve failure outputs instead and fix through a new additive commit/run.

---

## Completion verification

Before claiming `ZEREF-DAD-SON-001` complete, run the focused unit/contract suite plus the existing D001 stage/chat contract tests affected by reuse. Confirm `git diff`/GitHub compare shows only additive Dad/Son files and intended tests/workflow, with no canonical parent modifications. The final report must distinguish: frozen parent Zeref, trainable reconstruction ancestry, trained Dad/Son descendant, runtime ledger memory, derived corpus memories, and raw quantum evidence.
