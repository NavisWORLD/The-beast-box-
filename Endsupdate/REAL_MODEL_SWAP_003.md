# Endsupdate real model swap 003 — execution and comparison

**Experiment:** endsupdate-real-model-swap-003. **Candidate root:** Endsupdate/. **Decision:** HOLD until real paired corrected-vs-original task testing proves a benefit.

This reuses the frozen historical real-model A0 → B1 → A2 algorithm from the **copied** Endsupdate source, with a fresh experiment ID and new output directory. It executes genuine, SHA-pinned Model A (Zeref) and exact Model B (SmolLM2-135M at revision 4e53f736cbb20a9a0f56b4c4bf378d9f306ff915) only after verifying source hashes. The A-only, empty-memory and shuffled-memory controls remain unchanged. Preserved experiment 002 is read-only.

## Interpretation established before execution

All nine frozen structural gates must pass, Model A loaded-parameter identity must return exactly, A0/A2 restoration error must be zero for six frozen cases, and the new input-identity hashes must equal historical 002. The two runs' per-stage conditional NLL metrics are reported descriptively.

**No corrected CST performance advantage can be established by this A/B/A experiment:** the physical-energy candidate is not connected to operational dyn12. Its original and copied operational source implementations are identical. An output difference due to execution environment cannot be attributed to corrected CST. An observed B1 advantage over A0 would be a different-model measurement, not a corrected-mathematics effect. There is no automatic production promotion.

## Archived comparison and source identities

The verified historical result is from run 33914200592, artifact 9952563037; ZIP SHA-256 1ebdf098a542e44eaf54ad9e8fefe3c74fafaeb8c280c41a1369dd58f810fd2d; result SHA-256 0bc2daf23b82d82992412b60c0b03c0cbf520a7e20f1bbb8ded5957c59d26fab. Model A artifact 9670847045 (run 33132618727), ZIP SHA-256 8f27aabe3e1bac5edf1c51b958be1f50ff1d076105c3f41e3901d780e7aa3083. The source checkpoint and world SQLite/evidence/summary have separately pinned expected SHA-256 hashes in experiments/endsupdate-real-model-swap-003/protocol.json.

## Running the experiment

GitHub recognizes workflows only under repository-root .github/workflows/, not a copied Endsupdate/.github/workflows directory. The **single workflow exception** is .github/workflows/endsupdate-model-swap-003.yml. It reads from the frozen source artifacts and executes inside Endsupdate; no root runtime, memory, weights, evidence or existing workflow is modified. The workflow has read-only GitHub permissions and uploads generated JSON and failure logs only (never weights).

With verified input artifacts staged in Endsupdate/.endsupdate-model-input-003/source and the exact historical result at Endsupdate/.endsupdate-model-input-003/baseline/result.json, use these commands from Endsupdate:

    python -m unittest discover -s tests -p test_real_model_swap.py -v
    python run_real_model_swap.py --source-dir .endsupdate-model-input-003/source --historical-result .endsupdate-model-input-003/baseline/result.json --preflight-only
    python run_real_model_swap.py --source-dir .endsupdate-model-input-003/source --historical-result .endsupdate-model-input-003/baseline/result.json

A previously existing output directory is a hard stop; use a new experiment ID for additional runs, never delete old evidence. Model inference and output files must be generated by the run, not copied from historical evidence.

## Evidence status

The archived original run succeeded, but no fresh 003 success is claimed here before its own CI job completes. Inspect the new GitHub Actions run and its result.json/comparison.json and artifact hashes. If Model B cannot resolve or any mandatory source hash fails, record BLOCKED or FAILED, never PASS.
