# Endsupdate CST math experiment 001

Classification: **exploratory synthetic numerical comparison**, not externally preregistered, predictive, physical or model-performance validation. Fixed in `run_experiment.py`: Python standard-library binary64, seeds 17/29/43, three synthetic stellar-scale entities, mass and position perturbations ±5%, `Parameters()`, five arms (historical raw, corrected, eta-zero ablated, rotated-information shuffled, classical K+U). Capture exact scores, individual information contribution, invalid/edge behavior and the full numeric input. `run_experiment.py` writes deterministic sorted JSON; do not overwrite historical source evidence.

Hypothesis examined: whether repairing physical dimensions changes numerical output and whether the candidate's information feature changes the **reported score** under the chosen scales. Test permutation equivariance and singularity/invalid-input handling; report exact-score nulls. A changed score is not predictive gain. Historical and corrected outputs have different units; do not compare their raw magnitudes as accuracy. No external independent holdout or fitted parameters are included. Additional datasets, randomized schedules and precision analysis are required before any model-integration claim.
