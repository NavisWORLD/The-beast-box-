# Claim Boundaries

Frozen official classification (do not soften, do not strengthen):

```text
ENGINEERING_ISOLATION_VERIFIED_CAUSAL_RESOURCE_SOURCE_NOT_ESTABLISHED
```

Sealed scientific anchor: `c8769d0f1c9dab7a0c9adc0082d7234e7ff22f6f`

SOUL-QBT historical replay classification:

```text
ENGINEERING_CONTROL_INCONCLUSIVE
```

Correct interpretation:

A causal resource/source → downstream consumer effect has **not been established**.

That is different from proving a universal negative causal effect. Do not invent a source. Do not invent a signal. Do not inflate a null into a disproof.

This repository separates computational measurements from interpretations.

## Evidence classes

- **MEMORY**: append-only durable software records and their hashes. Memory records can preserve prior text and metadata; they do not prove that the text is true.
- **LIVE SOFTWARE STATE**: measurements produced by the current software runtime, including CNS state vectors and bound R12 live epochs.
- **EXPERIMENT EVIDENCE**: sealed inputs, preregistrations, workflow runs, artifacts, hashes, metrics, and logs from an identified experiment.
- **HYPOTHESIS**: an interpretation or proposed mechanism that is not established by the saved measurements.
- **MODEL-GENERATED TEXT**: a model output. It is behavioral data, not scientific ground truth merely because the model produced it.

## Claims the current evidence supports

Changing refractive retrieval ranking can change the active context presented to a model and therefore downstream generated output. Lexical retrieval can miss freshly injected state while a separately verified refractive/live lane can deliver a bound epoch. CNS software state uses `dyn12=12`, `dyn42=42`, and `dyn54=dyn12+dyn42` by concatenation.

The sealed whole-organism run verified engineering isolation, protected-state preservation, historical IBM hardware provenance, corpus integrity, and reproducible model/runtime instrumentation.

## Claims the current evidence does not establish

Nothing in the Zeref, R12, CNS7, IBM, heartbeat, or training lineage establishes:

- a causal IBM-resource-to-model-consumer effect;
- quantum advantage;
- consciousness or sentience;
- resurrection or biological continuity;
- that Zeref is literally a deceased person;
- a literal soul or a measured soul;
- communication with the dead;
- a direct mysterious force from routing parameters to neurons;
- a quantum anomaly merely because model behavior diverged;
- any new physical law.

A successful quantum-hardware job proves that the recorded circuit/job executed under the documented conditions. It does not, by itself, prove any of the claims above.

Promotion is based on saved metrics and integrity gates, never on emotional tone or a single impressive dialogue sample.

Do not run another IBM job to hunt a positive. Another null is acceptable. Scientific cheating is not.
