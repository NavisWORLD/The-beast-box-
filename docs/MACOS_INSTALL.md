# Zeref for macOS

This file is intentionally minimal while the macOS packaging tests are red. The final guide will document the verified `.app` and `.dmg` artifacts after the packaging workflow is implemented and exercised on GitHub-hosted macOS runners.
