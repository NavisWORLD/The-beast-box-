# Beast Box 0.6.0 — Swap the brain. Keep the story.

Release-hardened experimental software with model-independent continuity.

- Checked portable snapshot export, manifest verification and fresh-directory restore. Host credentials/configuration and tool authority do not travel with state.
- Improved `beastbox doctor`: real write/storage checks, checkpoint recovery, wheel RECORD integrity and explicit local model discovery.
- Durable compatible Chat Completions adapter for local servers and explicitly opted-in HTTPS endpoints. No cloud default, embedded key, redirect or inference fallback.
- Preserved README identity, one navigation block, five-minute setup, provider and portable-state guides, terminology and evidence-bound launch drafts.
- EnD's old version heading now points to source-bound release provenance. Python remains the core runtime; C++/Rust clients expose a JSON process interface.

Required release jobs test Python 3.10–3.12, installed wheel/sdist, desktop/native builds, Android emulator and iOS simulator, Linux-to-Windows portable-state handoff, and a separate real Ollama A→A→B→B→A story measurement. Read STORY_RECEIPT.json: memory delivery and exact fact emission are separate results. This demo does not modify the frozen scientific 002 record.

Download the combined ZIP and read QUICKSTART.md, or select the labelled desktop/mobile artifact. Verify SHA256SUMS.txt. Android is debug-signed for sideload testing. iOS includes simulator output and an unsigned device archive, not an installable signed iPhone app. Physical-device, USB filesystem and Apple distribution acceptance remain external gates. Remote provider and IBM/Azure account execution is not established by protocol tests. Optional inputs never establish quantum advantage.

State is plaintext, finite and owner-maintained. Portable export excludes host files and detects some recognizable credential content; it is not a universal secret scanner. Retain expected hashes and verified backups separately. Hashes are not signatures or a hostile-host security boundary. Model replacement does not guarantee personality preservation or improved recall.

Historical v0.3.2, v0.4.0 and v0.5.0 assets and all scientific failures/nulls remain unchanged.
