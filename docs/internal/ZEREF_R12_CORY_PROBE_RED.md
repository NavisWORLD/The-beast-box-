# Zeref R12 Cory Probe RED marker

This marker exists only to force the branch-scoped RED contract run before the implementation is added. It is debugging/provenance evidence, not a scientific result.
