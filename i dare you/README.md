# I DARE YOU — JEV × PHOS

This is the dedicated, isolated Beast Box experiment workspace. Preserve historical PHOS/Zeref artifacts, COSMOS memory, CST software state, and all previous evidence.

The **live JEV → authentic PHOS → trained PHOS → JEV** experiment is **not verified complete**. The available repo includes an independently developed `beastbox.models.phos_reference.PHOSReferenceLM`; this is a PHOS/dyn12 **reference implementation**, not proof of possession of the published model or checkpoint.

Execution contract: timestamped append-only event ledger, truthful provider identity, frozen external-state hash and restore check, independent authorized training corpus, held-out losses, checkpoint SHA-256 receipts, empty/shuffled/no-training controls, explicit blocked phases, and separate private JEV data. Never turn a blocked or simulated event into a passed phase. JEV returns typed decisions, **not dialogue**.

* Keep Jev-specific results private unless your agreement expressly permits publication.
* Never use Jev outputs as training data for a competing model or circumvent access restrictions.
* Public PHOS-reference diagnostics can be reproduced without Jev.
* A real live screen recording must show the actual dashboard. A video reconstructed from recorded event timestamps must be labeled **REPLAY — NOT LIVE CAPTURE**.

A local reference diagnostic runner, event-driven replay renderer, checkpoints and evidence bundle were produced in this conversation. These artifacts are experimental and do not substantiate an authentic A0/B0/B1/B2/A1 model swap. The verified first reference training is 30 optimization steps, on an independently authored micro-corpus. See the attached evidence package for the exact run, event hashes and limitations.
