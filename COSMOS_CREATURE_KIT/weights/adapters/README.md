# Adapter and state sidecars

Use this folder for CST/Trinity projection metadata, LoRA-style adapters when licensed, tokenizer sidecars, and state adapter manifests.

Adapters are tracked separately from backbone weights so a creator can compare the same backbone with and without COSMOS state machinery.
